/**
 * Re-exports `@/components/close/AutonomousCloseEngine` (tenant `primary_color` as `--tenant-glow`, G1, legal name). Prefer the `@/` import in app code.
 */
export { default } from "./src/components/close/AutonomousCloseEngine";
