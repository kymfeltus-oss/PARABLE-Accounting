# Copy to .env.local (same folder) — not committed.
# Supabase: Dashboard → Project Settings → API

NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Server-only (Edge Functions / scripts — never expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Optional: Next public app URL
# NEXT_PUBLIC_SITE_URL=http://localhost:3004

# Storage bucket for mandate PDFs (create in Supabase Dashboard → Storage → "mandate-documents")
